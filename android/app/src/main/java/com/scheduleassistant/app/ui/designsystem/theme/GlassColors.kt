package com.scheduleassistant.app.ui.designsystem.theme

import androidx.compose.ui.graphics.Color

/** 玻璃态下的强调色（与 mesh 色系协调） */
object GlassAccent {
    val primary = Color(0xFF2563eb)       // 品牌蓝（与应用主色一致）
    val primaryDark = Color(0xFF3b82f6)
    val success = Color(0xFF5EC4A7)
    val warning = Color(0xFFFFBF6E)
    val error = Color(0xFFFF8080)
}
